# Server
server.port=8080

# Database — PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/research_db
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# For local development with H2 in-memory (uncomment to use without Postgres)
# spring.datasource.url=jdbc:h2:mem:researchdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# Logging
logging.level.com.atrijahaldar=INFO
